import random
original=random.randrange(1,20)
guess=int(input("Enter the guess number:")
if guess=original:
  print("Correct Guess")
elif guess<original:
  print("Guess is low")
else:
  print("Guess is high")
print("original number is "+str(guess))
